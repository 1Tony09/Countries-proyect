const { Router } = require("express");
const countriesRouter = Router();

const {} = require("../handlers/countriesHandler");

countriesRouter.get("/", );
countriesRouter.get("/", );
countriesRouter.get("/", );

module.exports = countriesRouter;